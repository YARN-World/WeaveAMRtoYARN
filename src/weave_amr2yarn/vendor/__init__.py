"""Third-party code copied into the package.

``align2anchor`` is copied: it is not published, and its ``pyproject.toml`` sits inside the package
directory while naming the package, so it does not pip install as it stands.
"""